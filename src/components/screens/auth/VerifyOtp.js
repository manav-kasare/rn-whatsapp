import React from "react";
import { View, Text } from "react-native";

export default function VerifyOtp({ navigation, route: { params } }) {
  const { phone, verificationId } = params;
  return (
    <View>
      <Text></Text>
    </View>
  );
}
